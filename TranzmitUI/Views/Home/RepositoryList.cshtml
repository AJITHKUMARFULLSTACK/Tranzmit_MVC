@model IEnumerable<TranzmitUI.Models.RepositoryMaster>

<DOCTYPE! html>
    <html>
    <head>
        <title>REPOSITORY</title>
        <meta name="viewport" content="width=device-width" />
        <link href="~/Content/Gridmvc.css" rel="stylesheet" />
        <link href="~/Content/bootstrap.min.css" rel="stylesheet" />
        <script src="~/Scripts/gridmvc.js"></script>
        <script src="~/Scripts/jquery-1.9.1.min.js"></script>
        <link href="~/Content/main.css" rel="stylesheet" />
        <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.10.1/jquery.min.js"></script>
    </head>
    <body>
        <div style="margin-top:30px">
            <input id="myInput" type="text" placeholder="Search By Name.." style="margin-left:770px;border:groove;">
        </div>
        <div id="location">
            <table class="table" style="width:auto;margin-left:250px;font-size:15px;margin-top:30px;" cellpadding="2" cellspacing="2">
                <tr>
                    <th>REPCODE</th>
                    <th>SOURCE FOLDER</th>
                    <th>VERSION</th>
                    <th>FILENAME</th>
                    <th> </th>
                </tr>
                <tbody id="myTable">
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.RepCode)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.RepPath)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.RepVersion)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.RepName)
                            </td>
                            <td>
                                @Html.ActionLink("Update", "RepositoryUpdate", new { id = item.RepCode })|
                                @Html.ActionLink("Delete", "RepositoryDelete", new { id = item.RepCode }, new { onclick = "return confirm('Are sure wants to delete?');" })
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        <div class="btn btn-block" style="margin-left:550px">
            @Html.ActionLink("ADD", "RepositoryDirect", "HomeController", new { @class = "btn btn-secondary btn-block" })
        </div>
        <script>

            $(document).ready(function () {
                $("#myInput").on("keyup", function () {
                    var value = $(this).val().toLowerCase();
                    $("#myTable tr").filter(function () {
                        $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
                    });
                });
            });
        </script>


    </body>
</html>
